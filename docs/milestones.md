# マイルストーン

実行する前に、何が起きるか予測する。外れたらそこを調べる。

## Stage 0 — 動かす

- [x] host から guest を呼んで、文字列が出力される

わかったこと:
- 非決定性の入り口はコードを読んだだけでは分からない
    - 何を記録・制御すべきかは、ソースコードではなくビルド後のコンポーネントのimport一覧である

## Stage 1 — 乱数を入れる

- [x] guest で `wasi:random` を使う
- [x] 2回実行して出力が変わることを確認

わかったこと:
- WITに`import wasi:random/...`を1行足すだけで非決定性が入り込む
- コンポーネントのimport一覧には、意図して足したものとstdが自動で持ち込んだものがフラットに並ぶ
- importされていても、実際に呼ばれなければ出力には影響しない

## Stage 2 — 差し替える

- [ ] host 側で `wasi:random` を固定値を返す実装に置き換える
- [ ] 何度実行しても同じ出力になることを確認

わかったこと:

## Stage 3 — 記録と再生

- [ ] 記録: 本物の乱数を返しつつ、値をファイルに書く
- [ ] 再生: ファイルから読んだ値を返す
- [ ] 記録時と再生時で出力が一致することを確認

わかったこと:

## Stage 4 — 壊す

予測してから実行する。

- [ ] ログの値を書き換える → 予測:
- [ ] ログの順番を入れ替える → 予測:
- [ ] ログから1行削る → 予測:

わかったこと:

## あとで

- 時刻 (`wasi:clocks`)
- ファイル I/O
- CLI として使える形にする
